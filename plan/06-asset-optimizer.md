# 模組 6：資源優化器

## 目的

減少遊戲的下載大小。Web 遊戲的載入時間直接影響使用者體驗，檔案越小越好。

## 要做什麼

- PNG 圖片壓縮——在不明顯損失畫質的前提下減小檔案大小
- 音訊轉換——把 wav/mp3 轉成 ogg（Web 通用格式），降低 sample rate 和聲道數
- 優化結果放在 build 目錄裡，不動使用者的原始檔案

## 怎麼做

用外部工具（pngquant 壓 PNG、ffmpeg 轉音訊），透過 subprocess 呼叫。如果使用者沒有安裝這些工具，只印警告、不報錯、不擋住打包流程。

這個模組是可選的，使用者可以完全關閉。
